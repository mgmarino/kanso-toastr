kanso-jquery-mobile
===================

toastr wrapped for kan.so

This is currently version @VERSION@.  The toastr website is:

@URL@

Usage
-----

Typical usage:

    require("jquery");
    ...
 
    ...
    var toastr = require("toastr");

More information concerning the API for toastr is found on the above webpage.

